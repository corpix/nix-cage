# -*- mode: sh -*-
set -e
# set -x

proxy() {
    local v
    for v in `env | grep -i _proxy`
    do
        echo -en "export $v;"
    done
}

ac() {
    acbuild --debug "$@"
}

root="$(git rev-parse --show-toplevel)"
